# Per-task usage & cost accounting (`get_task_usage`)

**Status:** Implemented (this PR)

## Summary

Goal: a PM-only zero-arg MCP tool `get_task_usage` reporting the current task's token totals (always, aggregated crash-safely from all session transcripts) plus SDK-reported cost (from a new `shared/usage.jsonl`, verbatim from the SDK — no homegrown price table), with a per-agent breakdown and disclosed gaps. Approach: (1) `persistence.ts` gains `getUsageLogPath` + `appendUsageRecord` cloned from the events write-queue machinery, with a `query_nonce` field on each record; (2) a fire-and-forget, guarded hook in `spawn.ts`'s result-event branch appends `{ts, agentId, agentKey, query_nonce, session_id, subtype, num_turns, total_cost_usd, modelUsage, usage}` to `usage.jsonl`, where `query_nonce` is a `randomUUID()` generated once per `query()` call at the top of the while-loop; (3) a new pure `src/agents/task-usage.ts` recursively reads transcripts (dedup by `message.id`, skip `<synthetic>`, include nested subagents, exclude `journal.jsonl`) for tokens and aggregates `usage.jsonl` cost per-nonce (reduce within a nonce, sum across nonces) for cost; (4) the tool is registered in `createOrchestrationMcpServer` (PM-only). 6 tasks.

Key revision from the blocking findings: the earlier `decumulateCost` session_id drop-detection heuristic was over-engineered and could silently OMIT cost (a cheap `query()` call preceding an expensive one under a shared, resumed `session_id`), and depended on an unconfirmed session_id-stability assumption. It was replaced by a write-time per-`query()`-call nonce: cost = Σ over nonces of (within-nonce reduce of `total_cost_usd`), bucketed by the nonce's `agentKey` (so per-agent sums to grand by construction). This is materially simpler (deletes `decumulateCost`), provably correct with no omission, independent of session_id semantics, and reduces the empirical unknown to a single isolated within-nonce max-vs-sum choice that is documented-cumulative and unit-testable.

Verification: vitest unit tests cover AC1 (tool-contract 11→12 + repo server excludes it), AC2/AC3/AC4/AC7/AC8 (aggregation fixtures), AC6-unit (per-nonce reduce, the explicit no-omission spawn#1-cheap/spawn#2-expensive-same-session_id case, per-agent sums to grand) and the writer in isolation. AC5 and AC6 were VERIFIED post-ship on a real archie-e2e live boot of this branch (attested `32dd7f6`, PR #232; evidence at `e2e-evidence/basic-nonce`): a real result event appended a `usage.jsonl` record (AC5), and every `query_nonce` carried exactly one result-event record under both sequential turns and three messages queued mid-turn — so `max == sum` unconditionally, the cumulative-vs-delta fork is moot, and grand cost = Σ over nonces = per-agent sum with no false gap (AC6). No multi-record nonce arose, so within-nonce monotonicity was neither needed nor observed. Trade-offs: SDK cost is a disclosed client-side estimate (diverges under subscription auth); the cache 1h/5m split and `inference_geo` are not modeled (Archie sets neither — confirmed by grep); current-task only; PR #162 and `CHANGELOG` untouched. Fits one bounded PR plus one live boot.

## Brief

PM-only tool that answers "how much has this task used/cost so far?" for the CURRENT task. Per-message token usage is recorded in every agent's session transcript but never aggregated; SDK-computed cost arrives in the result event and is currently dropped at `spawn.ts:815`.

Goals: (1) A PM-only MCP tool reporting the current task's total token usage (always) plus SDK-reported cost (when available), with a per-agent breakdown. (2) Tokens are the source of truth, aggregated from ALL session transcripts (original + restarted + subagent), crash-safe. (3) Persist the SDK's per-turn cost to `shared/usage.jsonl` as the sole cost source.

HARD CONSTRAINT (operator directive): We build NO cost estimate and maintain NO price table of our own. Cost is whatever the SDK reports verbatim (`total_cost_usd` / `modelUsage.costUSD`). If the SDK did not report cost for a turn (task predates the hook, pre-hook turn, or a hard crash with no result event), cost is shown as `unavailable` — never inferred, never $0-filled, never computed from a homegrown rate table. Do NOT add a pricing module.

Non-goals (binding on reviewers): not real Anthropic billing (the SDK figure is itself a client-side estimate and diverges under subscription auth — disclose, don't correct); current task only, not cross-task/historical; do not remove or alter PR #162's temporary per-turn logging; `CHANGELOG.md` untouched (auto-generated); tool must remain PM-only (never reaches repo/plugin agents).

Design (from brief): (a) Tool `get_task_usage` (zero-arg) added to the tools array of `createOrchestrationMcpServer` in `src/agents/tools.ts` (PM-only; pattern `create<Name>Tool(agent, task)` returning `tool(name, desc, {}, handler)`, `ok()`/`err()` helpers at `tools.ts:183-184`). Handler has `task.taskId`; `getTaskPath(task.taskId)` = `sessions/<taskId>`. It RECURSIVELY reads every `*.jsonl` under `sessions/<taskId>/claude/*/session/projects/` INCLUDING nested `<sessionId>/subagents/**/agent-*.jsonl`, EXCLUDING `journal.jsonl`; on assistant lines sums `message.usage` (input_tokens/output_tokens/cache_creation_input_tokens/cache_read_input_tokens); DEDUPS by `message.id` (parallel tool calls emit duplicate ids with identical usage — keep one); SKIPS `message.model === '<synthetic>'`. No glob dependency exists — use fs readdir recursion (Node >=20). Do NOT enumerate via `metadata.agent_sessions` (it keeps only the latest session id per agent, missing restarts). (b) Persistence hook in the `spawn.ts` result-event branch (~L815, which currently reads only `event.subtype`): append `{ts, agentId (def.id), agentKey (def.key), session_id, subtype, num_turns, total_cost_usd, modelUsage, usage}` to `sessions/<taskId>/shared/usage.jsonl` via a new `appendUsageRecord` + `getUsageLogPath` in `src/tasks/persistence.ts` that clone the existing `appendEvent`/`getEventsLogPath` write-queue machinery (`persistence.ts:705-729`). Fire-and-forget and guarded — it must NEVER throw into or block the spawn loop, and must no-op if the shared dir is missing (mirror `appendEvent`'s existsSync guard). (c) Cost aggregation: from `usage.jsonl` ONLY. Use `total_cost_usd` (and `modelUsage.costUSD` for the per-model/per-agent split) — both include subagent usage; the SDK `usage` field EXCLUDES subagents so do not use it for cost. The per-turn-vs-cumulative summation semantics of successive result events within Archie's persistent streaming `query()` call MUST be determined empirically. Per-agent cost tagged by recorded `agentKey`. (d) Output: grand total (input/output/cache-read/cache-write tokens + SDK-reported cost or `unavailable`), per-agent breakdown with session count, cost labeled SDK-reported (SDK's own price table, not real billing; diverges under subscription auth), and a disclosed gap when cost-recorded turns < transcript turns.

## Acceptance criteria and methods

| AC | Criterion | Method | Status |
|----|-----------|--------|--------|
| AC1 | `get_task_usage` is registered exactly in the PM-only orchestration MCP server (12 tools) and is absent from the repo-tools server, so repo/plugin agents never receive it. | unit | verified |
| AC2 | Token totals sum the four buckets across all of an agent's session transcripts after dedup-by-`message.id` (keep first) and excluding `<synthetic>` turns. | unit | verified |
| AC3 | Nested subagent transcripts are included (rolled into the parent agent) and `journal.jsonl` is excluded. | unit | verified |
| AC4 | The report has a grand total plus a per-agent breakdown keyed by `agentKey`, with a per-agent session count. | unit | verified |
| AC5 | On a real SDK result event a usage record (`query_nonce`, `agentKey`, `session_id`, `total_cost_usd`, …) is appended to `sessions/<taskId>/shared/usage.jsonl`; a crash with no result event appends nothing. | live-e2e | verified |
| AC6 | Cost reduces within a nonce (`max`; in practice every nonce carries a single result-event record, so `max == sum`) and sums across nonces with no double-count/omission (incl. nonces sharing a `session_id`); per-agent costs sum to the grand total. | live-e2e | verified |
| AC7 | With no `usage.jsonl`, tokens still report and cost renders as `unavailable` (never $0), no throw; when cost-recorded turns < transcript turns, a disclosed gap line appears. | unit | verified |
| AC8 | Cost is labelled an SDK-reported figure (SDK's own price table, not actual Anthropic billing) and discloses divergence under subscription auth. | unit | verified |

## Design

The design keeps two independent data sources and joins them only at report time, because they have different availability and trust properties.

Tokens are the source of truth and are always available. The aggregator recursively reads every SDK transcript under `sessions/<taskId>/claude/<agentKey>/session/projects/` — including nested subagent transcripts and excluding `journal.jsonl` — dedups assistant lines by `message.id` (parallel-tool partials repeat identical usage), skips `<synthetic>` turns, and sums the four token buckets. Tokens bucket per top-level `agentKey`, so subagent tokens roll up into the parent agent. This is crash-safe: the SDK writes transcripts continuously regardless of whether a turn produced a result event, so tokens are recoverable even for turns that never reported cost.

Cost is SDK-reported and only present when the SDK emitted a `result` event. It is aggregated exclusively from a new append-only `sessions/<taskId>/shared/usage.jsonl`, populated by a fire-and-forget hook on the SDK `result` event in `spawn.ts`. There is deliberately no price table and no estimation — cost is the SDK's own `total_cost_usd` reported verbatim, shown as `unavailable` when no record exists.

### Files / subsystems

`src/tasks/persistence.ts` gains `getUsageLogPath(taskId)`, the `TaskUsageRecord` type, and `appendUsageRecord()`, all cloned from the `events.jsonl` pattern (`getEventsLogPath`/`appendEvent`). A dedicated module-level `usageWriteQueues` map serializes writes per task (kept separate from the events queue so the two never contend), an `existsSync` guard on `shared/` no-ops when the directory is missing, and the whole body is wrapped in try/catch so the fire-and-forget writer never throws.

`src/agents/task-usage.ts` is a new pure, unit-testable module holding the aggregation and formatting logic (`aggregateTaskUsage`, `formatTaskUsageReport`, and the injectable `reduceNonceCost`). It imports only `SESSIONS_DIR` from the workdir bootstrap so its test module graph stays a single mock, and builds all paths locally.

`src/agents/tools.ts` gains `createGetTaskUsageTool(agent, task)`, a `tool('get_task_usage', …, {}, handler)` that mirrors `createGetAgentsStatusTool`. It is registered in `createOrchestrationMcpServer`, which is wired only in the PM branch of `spawn.ts`, so the tool never reaches repo or plugin agents.

`src/agents/spawn.ts` generates one `randomUUID()` nonce per `query()` call inside the retry loop, in scope for that call's entire event loop, and appends a usage record (fire-and-forget, never awaited) on each `result` event.

### Cost aggregation semantics — why the nonce replaced the rejected `session_id` heuristic

The rejected design tried to reconstruct query()-call boundaries at READ time by grouping `usage.jsonl` records on `session_id` and segmenting each session's records into runs on a `total_cost_usd` drop (a `decumulateCost` step). Verified against the real code, that approach is both over-engineered and silently wrong. Archie makes exactly one `query()` call per spawn inside a `while (true)` retry loop, and an agent RESUMES the same `session_id` across many spawns (`existingSessionId = agent.session.session_id`, `resume: sessionId` in `buildQueryOptions`). So one `session_id` accumulates many independent query()-call cost windows.

The drop-heuristic OMITS cost whenever a cheap query() call precedes a more expensive one under a shared `session_id`. Concretely: spawn #1 is a $0.02 acknowledgement (query call A), the agent parks, spawn #2 resumes and its first turn costs $0.60 (query call B). The records `[0.02, 0.60]` show no drop, so A is absorbed into B's run and the $0.02 is lost — an omission the design must not have. Worse, the heuristic's correctness depended on an unconfirmed assumption (that `session_id` is stable across resume) that was never established.

The fix adopts a write-time nonce and deletes drop detection entirely. Each usage record is stamped with `query_nonce = randomUUID()`, generated once per `query()` call. A nonce cleanly delimits exactly one query() call's cost window and belongs to exactly one agent (each spawn is per-agent). Read-time cost becomes a two-level reduce with no ordering, no drop detection, and no dependency on `session_id` semantics: reduce within a nonce, then sum across nonces. Grand cost is the sum over nonces of `reduceNonceCost(nonce)`; per-agent cost is the same sum bucketed by the nonce's `agentKey`, and since every record in a nonce shares one `agentKey`, per-agent costs sum to the grand total by construction. The concrete undercount above is now impossible and is directly unit-tested: nonce A max $0.02 + nonce B max $0.60 = $0.62, no omission. `session_id` is kept on the record for traceability and debugging only, never for cost math.

### The empirical within-nonce reducer decision

Within a nonce, the records sharing one `query_nonce` must reduce to that query() call's single cost. Per the SDK cost-tracking docs, `total_cost_usd` is CUMULATIVE across the steps of a single query() call, so the default reducer takes the maximum — equivalently the final cumulative value, with `max` chosen because it is robust to line ordering and monotonic under the cumulative model. This is the one empirical fork in the design, and it is self-distinguishing: the cumulative hypothesis implies a monotonic non-decreasing sequence within a nonce, so any decrease within a nonce would prove the SDK is instead emitting per-turn deltas.

The reducer is injectable (`NonceReducer`), so the delta fallback — flip `max` to `sum` — is a one-line change confined to `reduceNonceCost` and is unit-tested independently. The choice only ever matters for a nonce with multiple result events; for a single-result-event nonce `max == sum`, so it cannot produce a wrong number. The live boot that was the AC6 hard merge gate resolved this in practice: every `query_nonce` carried exactly one result-event record — under both sequential turns and three messages queued mid-turn (a five-turn batch handled inside one `query()` call still produced a single record) — so no multi-record nonce arises, `max == sum` unconditionally, and the cumulative-vs-delta fork is moot. Within-nonce monotonicity was therefore never exercised or observed; the injectable reducer remains only as defensive headroom against a hypothetical future SDK that emits multiple result events within one `query()` call.

Across nonces the reduction is always SUM, which is documented rather than empirical: each query() call reports only its own cost, so there is nothing to deduplicate between nonces.

### Output shape

The report shows a grand total (input / output / cache-read / cache-write token buckets, plus `Cost (SDK-reported): $X.XX` or `Cost: unavailable`) followed by a per-agent breakdown with the same token buckets, a session count, and SDK-reported cost. Every cost figure is explicitly labelled as an SDK-reported estimate from the SDK's bundled price table — not actual Anthropic billing, and divergent under subscription auth where spend is flat. When the count of cost-recorded turns (usage.jsonl records) is below the transcript turn count (main-agent `end_turn` count), a disclosed gap line is appended noting how many turns the cost covers and that the rest predate cost logging or ended without a result event.

### Caveats (documented, not corrected)

SDK cost is a client-side estimate and diverges from real billing under subscription/flat auth — disclosed in output rather than corrected, per the non-goal.

Cache-write tokens are reported as a single bucket; the 1h-vs-5m ephemeral split (`ephemeral_1h/5m_input_tokens`) and `inference_geo` multipliers are NOT modeled, because Archie sets neither `ENABLE_PROMPT_CACHING_1H` nor `inference_geo` (confirmed: no such settings in `src/`; observed records carry `inference_geo:"global"` and `ephemeral_1h:0`).

The scope is the current task only. PR #162's temporary per-turn logging line is untouched, and `CHANGELOG.md` is left to its automation.

## Tasks

- **T1 — Add usage.jsonl writer to persistence.ts.** In `src/tasks/persistence.ts`, mirroring `getEventsLogPath`/`appendEvent` (705-729): export `getUsageLogPath(taskId) = join(getSharedPath(taskId),'usage.jsonl')`; export interface `TaskUsageRecord { ts; taskId; agentId; agentKey; query_nonce; session_id?; subtype; num_turns; total_cost_usd; modelUsage; usage }` (`query_nonce` is the per-`query()`-call identity used for cost aggregation; `session_id` is retained for traceability only); export `async function appendUsageRecord(record)` using a NEW module-level `usageWriteQueues = new Map<string,Promise<void>>()` (do not share the events queue), guarding `if(!existsSync(getSharedPath(record.taskId))) return;`, appending `JSON.stringify(record)+'\n'`, whole body in try/catch → `logger.warn`, never throws. Tests: new `src/tasks/__tests__/usage-persistence.test.ts` reusing persistence.test.ts's mock set — (a) appends one JSON line (incl. `query_nonce`) to `shared/usage.jsonl` when `shared/` exists and round-trips; (b) no-ops (no file, no throw) when `shared/` is missing.
- **T2 — Create pure aggregator `src/agents/task-usage.ts` (nonce-based cost).** New module importing only `SESSIONS_DIR`. Export `aggregateTaskUsage(taskId)` and `formatTaskUsageReport(report)`. TOKENS: recurse each `claude/<agentKey>/session/projects/` subtree via `fs.promises.readdir(dir,{recursive:true,withFileTypes:true})`; collect every `*.jsonl` except basename `journal.jsonl`, include nested `<sessionId>/subagents/**/agent-*.jsonl`; parse line-by-line (skip blank/malformed); for assistant lines skip `message.model==='<synthetic>'`, dedup by `message.id` (keep first), sum the four buckets into grand + per-`agentKey` buckets (subagents roll up to parent); `sessionCount` per agent = distinct `<sessionId>.jsonl` main files; `transcriptTurns` = distinct-id, `isSidechain!==true`, non-synthetic assistant lines with `stop_reason==='end_turn'`. COST from `usage.jsonl` only: group by `query_nonce` (missing → its own singleton, defensive); per-nonce cost = `reduceNonceCost(records)` whose DEFAULT is `Math.max` over `total_cost_usd`; `grandCost` = Σ per-nonce; `perAgentCost` = per-nonce bucketed by the nonce's `agentKey`; `costRecordedTurns` = total record count. No `session_id`-based de-cumulation. `formatTaskUsageReport` emits grand totals + per-agent breakdown, SDK-reported/not-billing/subscription-divergence label, `unavailable` when cost undefined, gap line when `costRecordedTurns < transcriptTurns`. Tests: new `src/agents/__tests__/task-usage.test.ts` — AC2, AC3, AC4, AC6-unit (single cumulative nonce → max/final; the no-omission $0.02 + $0.60 sharing one `session_id` → $0.62; per-agent sum == grand across two agents; sum-reducer fallback), AC7, AC8.
- **T3 — Register `get_task_usage` in the orchestration MCP server.** In `src/agents/tools.ts`: import `aggregateTaskUsage`/`formatTaskUsageReport`; add `createGetTaskUsageTool(agent,task)` modeled on `createGetAgentsStatusTool` (1042) — `tool('get_task_usage', <desc>, {}, async () => { try { return ok(formatTaskUsageReport(await aggregateTaskUsage(task.taskId))); } catch(e){ return err(...); } })`; register it in `createOrchestrationMcpServer`'s tools array (2641-2653). Tests: `tool-contract.test.ts` adds `'mcp__orchestration-tools__get_task_usage'` to `PM_ORCHESTRATION_TOOLS` (11→12); existing sorted-equality assertions for orchestration-tools (includes it) and repo-tools (`SPAWN_REPO_TOOLS`, excludes it) stay green → AC1.
- **T4 — Persist SDK result-event usage in spawn.ts (with per-`query()`-call nonce).** Add `appendUsageRecord` to the `../tasks/persistence.js` import (34-38) and `randomUUID` from `node:crypto`. Inside the `while (true)` retry loop (710), immediately before `const agentQuery = query({...})` (712), add `const queryNonce = randomUUID();` — one nonce per query() call, in scope for the whole for-await loop. In the event loop, after `task.noteActivityFromEvent(...)` (~L801) and before the `pendingTeardown` block, add `if (event.type==='result') { void appendUsageRecord({ ts, taskId, agentId:def.id, agentKey:def.key, query_nonce:queryNonce, session_id:event.session_id, subtype, num_turns, total_cost_usd, modelUsage, usage }); }`. Not awaited; never blocks. Does not touch PR #162's log line. Tests: `npm run typecheck` green; behavior verified live in T6 (AC5); the crash-path no-append is structural (spawn.ts:847-863 throws without a result event).
- **T5 — Docs: persistence.md + dated plan record.** `docs/architecture/persistence.md`: add `shared/usage.jsonl` to the directory tree and `getUsageLogPath` to the path-helper table; add a short "Usage & cost accounting" subsection describing the writer hook, the `query_nonce`-per-`query()`-call cost model, and the `get_task_usage` PM tool, stating tokens are source-of-truth from transcripts and cost is SDK-reported-verbatim (no price table), with the caveats (subscription-auth divergence; cache 1h/5m split and `inference_geo` not modeled because Archie sets neither; every `query_nonce` carries a single result-event record in practice — confirmed on the live boot — so `max == sum` and the cumulative-vs-delta reducer choice is moot). This dated plan record. `CHANGELOG.md` untouched.
- **T6 — Live QA via archie-e2e (AC5 + AC6) — DONE, post-ship.** A real archie-e2e live boot of this branch ran post-ship (attested `32dd7f6`, PR #232; evidence pair at `e2e-evidence/basic-nonce.json` / `.md`). Findings: (1) a real result event appended `usage.jsonl` records carrying `query_nonce`, `agentKey=pm`, `session_id`, `total_cost_usd` (AC5); (2) the boot established the single-record-per-nonce basis — every `query_nonce` carried exactly one result-event record under both sequential turns AND three messages queued mid-turn (the five-turn batch was handled inside one `query()` call and still produced a single record), so no multi-record nonce arises, `max == sum` unconditionally, and the cumulative-vs-delta fork is moot (within-nonce monotonicity was never exercised or observed, so the reducer needs no live confirmation of cumulative behavior); (3) cross-nonce SUM does not double-count — a resumed same-session query() call (turn 2) reported only its own fresh $0.06646225, not the cumulative ~$0.5192, despite sharing turn 1's `session_id`; and the tool's grand cost $0.51919 = $0.45272775 + $0.06646225 exactly, per-agent sum == grand, `costRecordedTurns` 2 == `transcriptTurns` 2 (no false gap). SDK-reported cost was sane against published Opus 4.8 rates (AC6).

## Verification plan

- **AC1 (unit).** Run `tool-contract.test.ts`: `createOrchestrationMcpServer(pmAgent)` registers exactly `PM_ORCHESTRATION_TOOLS` (12 entries incl. `get_task_usage`) by sorted equality, and `createRepoToolsMcpServer` registers exactly `SPAWN_REPO_TOOLS` (excludes it). Evidence: both sorted-equality assertions green; the 12-entry PM list including `mcp__orchestration-tools__get_task_usage`.
- **AC2 (unit).** Fixture: two `<sessionId>.jsonl` for one `agentKey`, duplicate `message.id` lines with identical usage, and one `message.model==='<synthetic>'` line. Assert the four token totals equal the hand-computed sums after dedup-by-id and excluding synthetic. Evidence: `task-usage.test.ts` token-bucket assertions against literal expected numbers; green.
- **AC3 (unit).** Fixture with a subagent transcript at `<sessionId>/subagents/.../agent-*.jsonl` plus a sibling `journal.jsonl`. Assert subagent assistant-usage tokens are included in totals and `journal.jsonl` contributes nothing. Evidence: total with subagent > total without; journal-only tokens absent; green.
- **AC4 (unit).** Fixture with two agents, one having two session files. Assert the report has a grand total and a per-agent map keyed by `agentKey`, and the two-session agent reports `sessions: 2`. Evidence: assertions on `report.grand` and the per-agent `sessionCount === 2`; green.
- **AC5 (live-e2e) — VERIFIED post-ship.** A real archie-e2e boot of this branch (attested `32dd7f6`, PR #232) completed a PM turn that fired a real SDK result event, and matching records were appended to `sessions/<taskId>/shared/usage.jsonl` — each carrying `query_nonce`, `agentKey=pm`, `session_id`, `subtype=success`, `num_turns`, `total_cost_usd`, and `modelUsage` (e.g. nonce `45ae1861`, session `3b0ceb69`, `total_cost_usd=0.45272775`). Crash sub-clause: verified structurally (spawn.ts:847-863 throws without a result event) plus the writer's own no-op-on-missing-shared unit test. Evidence: `e2e-evidence/basic-nonce` (live `usage.jsonl` dump) + `usage-persistence.test.ts` green.
- **AC6 (live-e2e, HARD gate) — VERIFIED post-ship.** On the same live boot the boundary question resolved to the single-record-per-nonce basis: every `query_nonce` carried exactly one result-event record under both sequential turns AND three messages queued mid-turn (the whole batch was handled inside one `query()` call and produced a single record with `num_turns=5`), so no multi-record nonce arises, `max == sum` unconditionally, and the cumulative-vs-delta fork is moot — within-nonce monotonicity was neither needed nor observed. Cross-nonce SUM does not double-count: a resumed same-session query() call (turn 2) reported only its own fresh $0.06646225, not the cumulative ~$0.5192, despite sharing turn 1's `session_id`. The tool's grand cost $0.51919 = $0.45272775 + $0.06646225 exactly, per-agent(pm) sum == grand, and `costRecordedTurns` 2 == `transcriptTurns` 2 (no false gap); SDK-reported cost was sane against published Opus 4.8 rates. Backed by unit fixtures: single-record nonce (max == final); the explicit no-omission case (nonce A $0.02 + nonce B $0.60 sharing one `session_id` → $0.62); per-agent sum == grand across two agents; injectable sum-reducer fallback. Evidence: `e2e-evidence/basic-nonce` (live `usage.jsonl` grouped by `query_nonce` + rendered tool output); AC6-unit assertions green.
- **AC7 (unit).** Fixture A: transcripts present, no `usage.jsonl` → tokens still reported, cost text is `unavailable` (not $0), no throw. Fixture B: `usage.jsonl` with fewer records than main-agent `end_turn` lines → the report contains a disclosed-gap line. Evidence: assertions on the formatted string (`unavailable` present, no throw; gap sentence present when `costRecordedTurns < transcriptTurns`); green.
- **AC8 (unit).** Render a report with cost present and assert the formatted output labels it an SDK-reported figure from the SDK's own bundled price table (not actual billing) and notes divergence under subscription auth. Evidence: substring assertions on the cost label/disclaimer lines; green.
