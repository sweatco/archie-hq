# Tasks: memory-v2-preflight

## 1. Replay script

- [ ] 1.1 Export the render paths from `src/memory/context.ts`: `renderEntityBlock` (or a thin `renderEntityBlocks(selection)` wrapper) plus the `<user_preferences>` and `<recent_activity>` wrappers (`renderUserPreferencesBlock(ref, content)` / `renderRecentActivityBlock(content)`), refactoring `buildMemoryContext` to call the exported helpers; typecheck + existing tests stay green, no behavior change
- [ ] 1.2 Write `scripts/memory-preflight.ts` skeleton: CLI args (`--limit`, `--report`, `--plugins-dir`); the snapshot location comes from `ARCHIE_WORKDIR` only — `WORKDIR` binds at module load (`src/system/workdir.ts`), so there is no positional-path mode — and the script refuses to run unless the workdir contains `memory/` (and `sessions/` for replay), so it can never silently read the real local workdir; load entities via `listEntities()` against the snapshot
- [ ] 1.3 Context reconstruction: enumerate `sessions/task-*/shared/metadata.json`, extract title + every agent that actually spawned (`agent_sessions` — the only field synced from real spawns; `participants` also gains entries on owner assignment without a spawn, so render it only as a labeled "assigned, possibly never spawned" supplementary row, never merged into the replay set); involved users by importing `readKnowledgeLog` (`src/tasks/persistence.ts`) and `extractUsernames` (`src/memory/lifecycle.ts`) directly — no copies; resolve repo/plugin selectors by agent kind: statically-defined agents from agent config under `--plugins-dir` (a local `archie-plugins` checkout at the tip of the prod-deployed `ARCHIE_PLUGINS_BRANCH`; parse agent frontmatter and each plugin's `.claude-plugin/plugin.json` in-script — the manifest `name` field is the `plugin` selector and agent ids map `<key>-agent` ↔ `agents/<key>.md`; do not import the production plugin loader, and never place `plugins/` inside the snapshot, since `scanPlugins` writes `plugins-data/` into `WORKDIR`), authoritative because only config carries `repo.primary` (`metadata.archie.primary` may name a non-first repo) and `plugin` selectors; dynamic agents from `dynamic_agents[].repos[0].github`; fall back to `metadata.repositories` only when it lists exactly one repo for the agent, emit an explicit ambiguous-primary partial-coverage marker when config is unavailable and metadata lists several, and mark genuinely unresolvable spawns as partial coverage
- [ ] 1.4 Replay + output: run each reconstructed context through `selectEntities` for selection detail and through the exported `buildMemoryContext` (called without `taskId`, so `recordSelection` stays gated off and the snapshot is never written) for the full-context token estimate; print per-spawn table (selected slug/score/scope, dropped, zero-signal, candidates, rendered-token estimate over the full context — user-preferences and recent-activity blocks included) and the coverage line (replayed N of M, skip reasons); run with the budget env values intended for prod and echo every env value that shapes rendered sizes (`ARCHIE_MEMORY_ORG_INJECT_MAX`, `ARCHIE_MEMORY_ENTITY_INJECT_MAX`, `ARCHIE_MEMORY_TOUCHED_BY_INJECT_MAX`), read back through `getOrgInjectMax()` / `getEntityInjectMax()` / `getTouchedByInjectMax()` rather than raw `process.env` so a misspelled variable surfaces as the default it produced, plus the `--plugins-dir` checkout's HEAD SHA, branch, and dirty flag, in the output header
- [ ] 1.5 Worst-case arithmetic block: rendered index tokens + `ARCHIE_MEMORY_ORG_INJECT_MAX` × largest org page + `ARCHIE_MEMORY_ENTITY_INJECT_MAX` × largest non-org page (post `touched_by` truncation) + summed rendered `<user_preferences>` blocks over every snapshot user file (production wrapper included, not raw file bytes; involved users are uncapped at runtime, so the all-users sum is the bound) + rendered `recent-activity.md` tokens, plus observed per-spawn max, tagged with the budget env values assumed (`ARCHIE_MEMORY_ORG_INJECT_MAX`, `ARCHIE_MEMORY_ENTITY_INJECT_MAX`, `ARCHIE_MEMORY_TOUCHED_BY_INJECT_MAX`) — formatted for pasting into the enablement PR
- [ ] 1.6 `--report` reading list covering every block the flag enables: entities by relation count / observation count / page bytes, staleness distribution, plus every `memory/users/*.md`, `recent-activity.md`, and the persisted `memory/entities/index.md` (injected verbatim as `<entity_index>`) with sizes; suspicious-content flags (URLs, imperative override phrasing, base64-like blobs) applied to entity pages, user files, recent-activity, and the rendered index alike

## 2. Snapshot schedule (laptop)

- [ ] 2.1 Write `scripts/snapshot-memory.sh`: wraps `pull-remote-data.sh -m` into `~/archie-snapshots/archie-memory-YYYYMMDD.tgz` (host/dir via env or args), skips if today's snapshot exists; passes the container name explicitly (the pull script's auto-detect uses `mapfile`, missing from the macOS `/bin/bash` 3.2 that launchd's default `PATH` resolves); appends each run's outcome to `~/archie-snapshots/snapshot.log`; launchd setup instructions (plist `PATH`, SSH-key assumptions) in the header comment
- [ ] 2.2 Install the launchd job on the laptop (daily `StartCalendarInterval` with `Hour`/`Minute` — not `StartInterval`, whose sleep-time firings are missed instead of coalesced on wake), verify one scheduled run lands a tarball

## 3. Run the pre-flight (human + script)

- [ ] 3.1 Pull a fresh full snapshot (`pull-remote-data.sh` with sessions) from prod and run the replay; save output; verify read-only-ness: capture the full tree listing including directories (`find <snapshot> | sort`) and the file hashes (`find <snapshot> -type f | sort | xargs shasum`) before and after the full replay + `--report` run and confirm both are identical — hashes alone cannot detect forbidden new directories (e.g. an accidentally created `plugins-data/`)
- [ ] 3.2 Human store review (~1–2h) covering every block the flag enables — the `--report` reading list (entities, index) plus every `memory/users/*.md` and `recent-activity.md`: junk / staleness / prompt-injection residue; record the junk list and clean up flagged pages on the prod store (housekeeping `--target` or manual edits run on the prod host, e.g. via `docker exec` — editing the local snapshot changes nothing prod would inject), only while the app is stopped or verifiably idle: out-of-band edits bypass the in-process serialized write queue (`src/memory/lifecycle.ts`) and a concurrent extraction's read-modify-write can silently revert or corrupt them; after manual page edits, rebuild the entity index (or use housekeeping-mediated cleanup, which maintains it) so the persisted `entities/index.md` that prod injects verbatim is not left stale; then pull a fresh snapshot — taken after confirming no extractions ran since the edit — and re-run the replay, checking both the selections and the rendered `<entity_index>` content, to confirm the cleanup landed, before enablement
- [ ] 3.3 Judge: selections sane + worst-case tokens acceptable → go; else adjust (budgets via flags, store cleanup) and re-run replay

## 4. Docs + roadmap sync

- [ ] 4.1 Add a short pre-flight/snapshot note to `docs/architecture/memory.md` (tooling section; replay script marked disposable)
- [ ] 4.2 Update `docs/proposals/memory-v2-roadmap.md` Phase 1 item statuses (pre-flight items → shipped as they land)

## 5. Enablement (separate PR, human)

- [ ] 5.1 Enablement PR: flip `ARCHIE_MEMORY_INJECT=true` in prod config with the replay's worst-case arithmetic + store-review outcome in the description; rollback = flag off
- [ ] 5.2 Watch sensor telemetry for 2–3 weeks via scheduled `-m` pulls; tune `ARCHIE_MEMORY_ORG_INJECT_MAX` / `ARCHIE_MEMORY_ENTITY_INJECT_MAX` by flag from observed records
