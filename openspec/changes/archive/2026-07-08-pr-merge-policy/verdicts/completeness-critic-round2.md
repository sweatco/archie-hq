# Completeness verdict: pr-merge-policy — round 2

Reviewed: verdicts/completeness-critic-round1.md against the revised proposal.md, design.md, tasks.md, specs/pr-merge-policy/spec.md, specs/debug-mcp-task-waiting/spec.md, specs/archie-e2e-harness/spec.md, verification-plan.md, brief.md. Cross-checked the fix targets outside the change folder: `/Users/khmelev/Projects/swc/archie-plugins/pm/skills/engineering-team/SKILL.md` (lines 28 and 78 confirmed stale as claimed) and the archived main specs `openspec/specs/debug-mcp-task-waiting/spec.md` / `openspec/specs/archie-e2e-harness/spec.md` (requirement headers match the deltas' MODIFIED headers, so both deltas apply cleanly).

Orchestrator disposition audited: gaps 1–8 all claimed fixed, nothing accepted-without-fix.

## Gap-by-gap verification

| R1 gap | Status | Where the fix landed (verified concrete) |
|--------|--------|------------------------------------------|
| 1 (blocking) — live PM skill teaches auto-merge | **Closed** | New task 8.4 names the full path `/Users/khmelev/Projects/swc/archie-plugins/pm/skills/engineering-team/SKILL.md`, quotes both stale passages verbatim (line 28 lifecycle step 6 "**Merge** → System auto-merges when approved + CI passes"; line 78 "auto-merge handles the green path"), prescribes the rewrite (ready-notification relay once, delegate on request, approve/deny buttons, `autoMerge: true` keeps old behavior, green-CI wakeups now serve the ready notification), and ships in the same archie-plugins docs PR as 8.3 coordinated with the engine deploy. design.md D9 fourth bullet mirrors this; proposal.md Impact names the skill explicitly. Both passages verified present in the live file at those lines. |
| 2 (blocking) — missing debug-mcp-task-waiting delta | **Closed** | `specs/debug-mcp-task-waiting/spec.md` now exists: MODIFIED "Block until a terminal or actionable state" (header matches the main spec exactly), widening only the enumeration — prose "approval gates (edit mode, research budget, merge)" and the approval-gate scenario's type list gains `merge`; all other text and the four sibling scenarios preserved verbatim. proposal.md Modified Capabilities corrected to list `debug-mcp-task-waiting` (the round-1 "Modified Capabilities: None" claim is gone). |
| 3 — e2e harness spec literalism vs deny recipe | **Closed** | `specs/archie-e2e-harness/spec.md` MODIFIES "Handle the edit-mode approval gate" (header matches the main spec): "SHALL approve" generalizes to "SHALL resolve the gate … with the decision the scenario's recipe prescribes — approve by default, deny where the recipe exercises a denial path", "proceed to completion" → "proceed to the recipe's expected state"; original approve scenario kept, new "Recipe-prescribed denial resolves the gate" scenario names `merge-approval-deny`. proposal.md Modified Capabilities lists it. |
| 4 — no test for merge API failing after approval | **Closed** | tasks.md 3.5 adds the exact case: "approve + mergeable but `mergePullRequest` returns `{success: false}` or throws → no completion finding, decision finding carries the failure message, slot cleared, PM reactivated". verification-plan.md AC4 row adds matching case (c). design.md D7 already promised the failure-message finding; now tested. |
| 5 — ready-notification vs pending-approval double prompt | **Closed** | design.md D4 final sentence: held PR whose `pending_merge_approval` matches is skipped by the notification step. tasks.md 5.4 wires the skip; 5.5 adds the test ("ready PR matching a pending `pending_merge_approval` slot → no notification"). Spec scenario "Pending merge approval suppresses the ready nudge" added; verification-plan AC1 row carries the companion case. |
| 6 — stale Slack button can resolve a different PR | **Closed** | design.md D6 step 4 puts `<taskId>\|<github>#<pr_number>` in the button value; D7 verifies clicks against the slot — mismatch resolves nothing and marks the prompt stale. tasks.md 6.1 (parse + verify + stale-notice message update) and 6.3 (mismatch test case). Spec: "Slack approval buttons SHALL carry the identity of the PR…" + scenario "Click on a superseded prompt is a no-op". |
| 7 — rollback with in-flight merge approval strands the task | **Closed** | design.md Migration Plan adds the caveat verbatim (old code 400s on `type: "merge"`, no `approve_merge` handler) and the operator remedy: deny pending merge approvals via `POST /tasks/:id/approve` before rollback, or nudge the thread after. That is the "one sentence in the Migration Plan" the gap asked for; operator awareness lives in the same doc an AC9-day operator consults. |
| 8 — AC1's "PM posts exactly one" unverified LLM half | **Closed** | verification-plan.md AC9 checklist gains observation (1): thread received exactly one "PR ready — ask me to merge" post for the ready period, explicitly labeled AC1's user-visible half; observation (2) checks the PM relay against the updated skill (also tying gap 1's fix to live verification). AC1 row now states the residual proxy openly ("engine-side once-ness … is a proxy for the PM's single Slack post") and points at task 8.4 + AC9. |

## Delta scan (new gaps / contradictions introduced by the revision)

Checked: task numbering and cross-references (8.1–8.5 coherent; verification-plan's "task 8.4" points at the right task; 2.4→4.1 dependency intact; 9.1 recipe matches the AC3 row step-for-step), spec scenarios vs design decisions (supersede-on-later-turn: D6 step 2 + D7 ↔ spec "Stale pending request is superseded" ↔ tasks 4.2/4.3 all agree; same-PR click on a superseded prompt intentionally resolves per D7, and the spec's no-op scenario is correctly scoped to mismatched identity only), verification-plan rows vs tasks (AC1–AC8 evidence files all named by a task or co-located convention), proposal Modified Capabilities vs the two delta files (accurate summaries), and both MODIFIED deltas vs the archived main specs (headers match; changes are the minimal claimed widenings).

One minor divergence found:

1. **non-blocking** — **Marker-clearing wording: tasks 5.3 says "open-but-not-ready", spec and design say "not ready".** specs/pr-merge-policy/spec.md ("cleared when a merge check observes the PR no longer ready") and design.md D5 ("cleared whenever a merge check observes that PR not-ready") put no `open` qualifier on the clear rule; tasks.md 5.3 narrows it to "entries whose PR is observed open-but-not-ready". In the exotic closed-then-reopened-still-ready case the task wording keeps the marker set (no re-notification) while the spec wording would re-notify. An implementer following design + spec gets the better behavior; align 5.3's phrasing (drop "open-but-") or scope the spec sentence. Noise-floor edge case, no AC affected.

## Verdict

**PASS.** All 8 round-1 gaps (2 blocking, 6 non-blocking) are concretely closed in the named artifacts — no vague mentions, no silent acceptances; the orchestrator's disposition is accurate. One new non-blocking wording nit (item 1 above) between tasks.md 5.3 and the spec/design clear-rule; fix is a two-word edit, no re-review needed.
