# QA evidence audit — pr-merge-policy, round 1

Auditor: independent QA reviewer (fresh context). Date: 2026-07-06. Method: judged the recorded evidence against the brief's ACs and the verification plan — not the runner's own claims. All suite test files named in `qa-evidence/suite-evidence.md` were independently re-run with `npx vitest run <file> --reporter=verbose` to confirm the named cases exist and pass. No `src/` implementation was read.

## Rulings

| AC | Ruling | Basis |
|----|--------|-------|
| AC1 | **VERIFIED** | All five named cases exist and pass in `src/connectors/github/__tests__/merge.test.ts` (suite `checkAndMergeLinkedPRs — non-auto policy (AC1)`): webhook-burst once-ness with fresh `Task.get` per run, pending-approval nudge suppression, un-ready→ready marker clear/re-notify, merged-PR marker clear, reload persistence. Matches the plan's scenario, companions included. The user-visible half ("PM posts exactly one Slack message") is an LLM behavior the plan explicitly scopes out of pre-merge evidence and routes to the AC9 checklist — the evidence honestly declares this proxy rather than papering over it. |
| AC2 | **VERIFIED** | Both named cases exist and pass in `merge.test.ts`: `merges a ready PR in an auto repo as today, with no ready notification` (suite `— auto policy (AC2)`) and the mixed-policy case `merges the auto PR while the non-auto PR is held with a ready notification` — the plan's open-question companion is covered, not just the happy path. |
| AC3 | **VERIFIED** | Evidence pair `merge-approval-deny.json`/`.md` demonstrates every clause of the criterion with real observed values, not tautologies. Details below. Minor hygiene gap noted (teardown line) that does not undermine the criterion. |
| AC4 | **VERIFIED** | All plan cases (a)–(d) exist and pass in `src/tasks/__tests__/merge-approval.test.ts`: ready→merge+finding+PM reactivation; dirty and closed refusals with reason; `{success:false}` and thrown-error failure surfacing with slot cleared; supersede-during-resolution race pair plus denial identity mismatch. Tool-side pre-check companion (`non-auto not-ready: returns the not-ready text without prompting`) confirmed in `src/agents/__tests__/pr-tools.test.ts`. |
| AC5 | **VERIFIED** | Both no-floor cases exist and pass: `zero review approvals but clean: approval is requested, not refused (AC5)` (pr-tools) and `merges with zero review approvals when GitHub reports clean — no approved floor (AC5)` (merge-approval). Full `isMergeReadyPerGithub` truth table (6 cases) passes in `src/connectors/github/__tests__/mergeability.test.ts`; no case name references review approvals. |
| AC6 | **VERIFIED** | `auto repo: merges directly with no approval request and no pause (AC6)` and the aligned-condition case `merges when blocked but mergeable=true (aligned to orchestrator Rulesets tolerance)` both exist and pass in `pr-tools.test.ts`. |
| AC7 | **VERIFIED** | File-name deviation from the plan (`plugin-loader-frontmatter.test.ts` instead of `plugin-loader.test.ts`; `registry-auto-merge.test.ts` for the registry/policy tests) is declared and is naming-only — every planned behavior is covered by a passing named case: plural/singular frontmatter defaults, non-boolean fail-safe (`"true"` string, numeric 1), registry copy default, `synthesizeDynamicAgentDef` always-false, `isAutoMergeRepo` undeclared/not-unanimous → false. Live corroboration: the AC3 run gated a merge on `sweatco/archie-plugins` (Archie's own plugins repo) under unchanged frontmatter. |
| AC8 | **VERIFIED** | Deviation declared and within the plan's own allowance ("or co-located events test"): `src/connectors/__tests__/merge-approval-surfaces.test.ts` covers both surfaces in one suite. All seven named cases exist and pass, including the load-bearing ones: approve/deny "both surfaces call the same handleMergeApproval/handleMergeDenial with the same parsed identity", Slack stale-button disposition, API 409 stale, API 400 on missing `github`/`pr_number`. Live corroboration of the API half via the AC3 run's real deny. |
| AC9 | **WAIVED-OK** | Waiver matches the brief's pre-accepted QA limitation verbatim. The post-merge step is credible and named: operator (Egor), concrete procedure (real PR, real Slack Approve click, confirm merge + thread notification), two piggybacked observations closing AC1's user-visible half and the PM-relay skill check, and a concrete evidence location (PR comment with task id, Slack permalink, merged-PR link; `forge.yaml` marked afterwards). |

## AC3 detail (live-e2e pair)

- **Assertions are the right ones and non-tautological.** The three criterion clauses each map to a dedicated assertion with concrete observed values that go beyond the expected text: `merge-gate-surfaced` (tool did not merge; `approval:requested` at 12:59:37.074Z with `approvalType: "merge"`, followed by `task:stopped` — the pause; `wait_for_task` returned `STATE=approval_requested APPROVAL_TYPE=merge` per the brief's required observation channel); `deny-resolves-via-api` (identity-carrying deny, `approval:resolved {type: merge, approve: false}` at 13:00:03.905Z); `no-merge-occurred` (denial finding in the knowledge log, no merge finding/event anywhere, **and external corroboration**: `gh pr view 81 --repo sweatco/archie-plugins` → `mergedAt: null`, PR later closed unmerged with branch deleted — the strongest possible non-merge proof, independent of Archie's own event stream).
- **Excerpts back the assertions.** The knowledge-log excerpt carries the nonce, the merge-request line, the agent's own report that the tool "posted a merge-approval request and paused the task", the denial finding, and the PM's final "remains open and unmerged" reply. The events excerpt shows the full ordered sequence: edit-mode request/resolve → `pr_card` (PR #81, real head SHA) → merge `approval:requested` → `task:stopped` → `approval:resolved` (deny) → `task:completed`. Timestamps are mutually consistent between log and events.
- **Result field integrity.** `result: "pass"` equals the AND of the 5 assertion passes (all true), consistent with the validator contract; each assertion's `observed` is a distinct factual record, not a restatement of `expected`.
- **Boot attestation: present.** `Attested: instance composed from 1cf34ce9…` with `/health` reporting the identical `git_sha`, matching `git rev-parse HEAD` on a clean tree — the instance under test was built from the branch under review, ruling out stale-build results.
- **Teardown line: absent.** Neither the JSON nor the MD contains the skill's `Teardown clean: docker compose ps --all reports no containers for this project.` confirmation. The `archie-e2e-evidence/v1` schema has no teardown field (the skill only calls the line "suitable for pasting into evidence"), and the boot attestation already excludes the stale-instance risk teardown hygiene guards against — so this does not weaken the AC3 demonstration itself. Flagged as a process gap: future evidence should paste the teardown confirmation, per the skill's "never end a scenario session without a clean teardown exit."

## Independent re-run summary

- `merge.test.ts` + `mergeability.test.ts`: 13/13 pass — matches suite-evidence exactly.
- `merge-approval.test.ts` + `pr-tools.test.ts`: 43/43 pass — all AC4/AC5/AC6-tagged case names present verbatim.
- `plugin-loader-frontmatter.test.ts` + `registry-auto-merge.test.ts` + `merge-approval-surfaces.test.ts` + `tools/debug-mcp/wait-for-task.test.ts`: 36/36 pass — all AC7/AC8 case names present verbatim, plus the AC3-backing `surfaces a merge approval gate with its deferred stop (APPROVAL_TYPE=merge)` observability case.

## Overall

8 VERIFIED, 1 WAIVED-OK, 0 UNCONVINCING. Evidence quality is high: suite evidence names real, verbatim-matching test cases (re-run independently, all green), declares its two file-name deviations honestly and both are benign, and the live AC3 pair carries concrete timestamps, a boot attestation, and external GitHub-side corroboration of non-merge. Sole deficiency: the missing teardown confirmation line in the AC3 evidence — a hygiene gap, not an evidentiary one.
