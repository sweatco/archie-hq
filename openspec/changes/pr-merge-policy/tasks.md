## 1. Frontmatter flag and type threading (AC7)

- [x] 1.1 Add `autoMerge?: boolean` to `PluginRepoEntry` (`src/system/plugin-loader.ts:92-96`) and populate it in the plural-shape map (`plugin-loader.ts:293-296`) and the legacy-singular migration (`plugin-loader.ts:310-319`), strict-boolean: only `entry.autoMerge === true` yields `true`
- [x] 1.2 Add `autoMerge: boolean` to `RepoEntry` (`src/types/agent.ts:84-89`) and thread it through the registry copy (`src/agents/registry.ts:81-86`) as `autoMerge: r.autoMerge === true`; hardcode `autoMerge: false` in `synthesizeDynamicAgentDef` (`registry.ts:190`)
- [x] 1.3 Tests in `src/system/__tests__/plugin-loader.test.ts` (new frontmatter-parsing describe) or a new co-located test: plural entry with `autoMerge: true` parses true; absent → false; non-boolean (`"true"`, `1`) → false; legacy singular `metadata.archie.repo.autoMerge: true` migrates; registry test (`src/agents/__tests__/`) asserting `RepoEntry.autoMerge` defaults false and dynamic agents are always false (AC7)

## 2. Policy lookup and shared mergeability helper

- [x] 2.1 Add `isAutoMergeRepo(github: string): boolean` to `src/agents/registry.ts` beside `findAgentDefsContainingRepo` (`registry.ts:178`): false when no registered agent declares the repo; true only when every declaring agent's matching entries all set `autoMerge === true`; `logger.warn` once per call on mixed flags
- [x] 2.2 Unit tests for `isAutoMergeRepo`: undeclared repo → false; single agent true → true; single agent absent → false; two agents true+true → true; two agents true+false → false (and warn logged); agent declaring the repo twice with mixed entries → false
- [x] 2.3 Create `src/connectors/github/mergeability.ts` exporting `isMergeReadyPerGithub(status: PRStatus): boolean` (`clean` OR `mergeable === true && blocked`), with `import type` for `PRStatus`; unit tests for the four state combinations plus `dirty`/`unstable` → false
- [x] 2.4 Switch the orchestrator's mergeable-bucket condition (`src/connectors/github/merge.ts:111-117`) and the tool's readiness check (`src/agents/tools.ts:1473`) to the shared helper; update `src/agents/__tests__/pr-tools.test.ts` so `blocked + mergeable=true` now merges (tool aligned to orchestrator); `npm run typecheck` and `npm test` green

## 3. Merge approval type and pending state (AC4, AC5 resolution side)

- [x] 3.1 Widen the `postInteractiveToUser` approvalType union (`src/tasks/task.ts:585`) to `'edit_mode' | 'research_budget' | 'merge'`
- [x] 3.2 Add `pending_merge_approval?: { github: string; pr_number: number; requested_by: string; requested_at: string }` to `TaskMetadata` (`src/types/task.ts`, beside `edit_allowed`); round-trip persistence test in `src/tasks/__tests__/persistence.test.ts`
- [x] 3.3 Implement `Task.handleMergeApproval(approver, expected: {github, pr_number})` (mirror `handleEditModeApproval`, `task.ts:1232`, plus an atomic identity gate): **synchronous read-compare-clear** on `pending_merge_approval` — read the slot, compare it to `expected`, and clear it on match, with no await between the three steps; empty or mismatched slot → warn + return a stale disposition (no merge, slot untouched) so the calling adapter can mark the prompt stale; on match only: clear the requesting agent's parked teardown via `requested_by`, fetch PR status, merge via `GitHubClient.mergePullRequest` when `state === 'open' && isMergeReadyPerGithub(status)` — **no `approved` check** — append completion finding on success / decision finding with the exact reason on failure, save, reactivate PM via `sendMessage(AGENT_PROMPTS.existingTask, 'pm-agent')`
- [x] 3.4 Implement `Task.handleMergeDenial(expected)`: same synchronous read-compare-clear gate (empty or mismatched slot → warn + stale disposition, nothing cleared); on match clear slot + parked teardown, append `Merge denied by user — PR not merged` finding, reactivate PM; no GitHub calls
- [x] 3.5 Tests (new `src/tasks/__tests__/merge-approval.test.ts`, mocked `GitHubClient`): approve + mergeable → merge called + completion finding + PM reactivated (AC4); approve + not mergeable (`dirty`, closed) → no merge + reason finding + PM reactivated (AC4); approve + mergeable but `mergePullRequest` returns `{success: false}` or throws → no completion finding, decision finding carries the failure message, slot cleared, PM reactivated so the failure reaches the user (AC4); approve with `approved: false` in status but `clean` → merge proceeds (AC5); deny → no `getPRStatus`/`mergePullRequest` call, slot cleared; stale resolution (empty slot) → no-op; supersede-during-resolution race (blocking red-team round-2 fix): slot rewritten to PR#2 (simulated supersede) before `handleMergeApproval(approver, {PR#1})` runs → **no merge of either PR**, slot still holds PR#2, stale disposition returned, and a subsequent `handleMergeApproval(approver, {PR#2})` still resolves PR#2 normally; same mismatch case for `handleMergeDenial`

## 4. Tool policy gating in `merge_pull_request` (AC3 plumbing, AC5, AC6)

- [x] 4.1 Rework `createMergePRTool` (`src/agents/tools.ts:1458-1481`): `isAutoMergeRepo(github)` → existing direct-merge body (with shared helper, task 2.4) (AC6); non-auto → approval-request branch; update the tool description string (`tools.ts:1461`) to state the policy-gated contract (auto repo merges directly after the mergeability check; non-auto repo posts a merge-approval request to the user and pauses the task)
- [x] 4.2 Approval-request branch: suppression/supersede fork applies only when `pending_merge_approval` is set (empty slot + parked teardown from another approval type → proceed to post the merge prompt normally); duplicate suppression gated on task-level quiescence — if **any** agent process in the task has a pending teardown (`[...task.agentProcesses.values()].some((a) => a.pendingTeardown)`, the same predicate `idleDecision` uses at `src/tasks/recovery.ts:100`; not just the caller's own `agent.pendingTeardown`, so a concurrent second repo agent cannot supersede a seconds-old request) → informational "already pending" return, no second prompt; a `pending_merge_approval` slot found while no agent in the task holds a pending teardown (task reactivated without resolution) is superseded — rewrite the slot for the requested PR and post a fresh prompt (design D7 cancel path); require `state === 'open' && isMergeReadyPerGithub(status)` with **no `approved` requirement** (AC5), else return today's "not ready" text; append decision finding; post interactive blocks (action ids `approve_merge`/`deny_merge`, value = `<taskId>|<github>#<pr_number>`) via `postInteractiveToUser(..., 'merge')`; persist the slot; `task.suspendStatus()`; `agent.deferTeardown(() => task.stop())` (pattern at `tools.ts:489-570`)
- [x] 4.3 Tests in `pr-tools.test.ts`: auto repo → direct merge, no `approval:requested`, no pause (AC6); non-auto ready → no merge call, slot persisted, teardown deferred, interactive post with type `merge` (AC3 plumbing); non-auto not-ready → "not ready" text, no prompt; same-turn duplicate request → single prompt; concurrent-agent case (non-blocking red-team round-2 fix): slot set and a *different* agent process in the task holds the pending teardown → informational already-pending return, no supersede, no second prompt; stale slot with no agent in the task holding a pending teardown → superseded: slot rewritten and fresh prompt posted, for both the same PR and a different PR; zero-approvals-but-clean in non-auto → approval requested, not refused (AC5)

## 5. Orchestrator gating and ready notification (AC1, AC2)

- [x] 5.1 Add `merge_ready_notified?: boolean` to `BranchState` (`src/types/task.ts:161`)
- [x] 5.2 In `triggerMergeCheck` (`src/connectors/github/merge.ts`): split the mergeable bucket by `isAutoMergeRepo`; merge only the auto subset (AC2); add `ready: string[]` to `MergeCheckResult` for held PRs; extend categorization logging with `READY (merge on request)`
- [x] 5.3 Implement notify-once: map each held PR back to its `BranchState` entries (scan `task.metadata.repositories` by `github` + `pr_number`); "already notified" iff any entry carries `merge_ready_notified`; clear the marker on entries whose PR is observed no longer ready — not-ready-while-open or closed without merging — so a closed-then-reopened PR notifies again (continuous-ready-period semantics)
- [x] 5.4 Add `notifyPMAboutReadyPRs(taskId, prs)` (modeled on `notifyPMAboutConflicts`, `merge.ts:234-250`): decision finding instructing the PM to tell the thread the PR is ready and merges on request, then reactivate PM; wire it into `checkAndMergeLinkedPRs` for newly-notifiable PRs only, setting markers on all matching entries; skip a PR whose `github` + `pr_number` matches the current `pending_merge_approval` slot — its approval prompt is already in front of the user, no double nudge
- [x] 5.5 Tests (new `src/connectors/github/__tests__/merge.test.ts`, mocked `GitHubClient` + task fixture): non-auto approved+clean → no `mergePullRequest`, exactly one notification across two consecutive `checkAndMergeLinkedPRs` runs (webhook-burst simulation, AC1); auto approved+clean → squash merge as today (AC2); mixed-policy task → auto PR merges while non-auto PR is held (per-PR policy); un-ready → ready again → second notification (marker cleared); marker survives task reload (persisted); ready PR matching a pending `pending_merge_approval` slot → no notification (AC1 companion)

## 6. Resolution surfaces: Slack, API (AC8)

- [x] 6.1 Add `approve_merge`/`deny_merge` Bolt action handlers in `src/connectors/slack/events.ts` (after the edit-mode pair, `events.ts:217-290` pattern with one ordering change): ack, parse the button value (`<taskId>|<github>#<pr_number>`) into `expected = {github, pr_number}`, `Task.get`, approver resolution via `getUserInfo` with the `isExternalUser` bail-out (external approver still resolves the approval; identity omitted from the finding), then `task.handleMergeApproval(approver, expected)` / `task.handleMergeDenial(expected)` — the handler does **no** slot verification of its own (the Task method's atomic read-compare-clear is the single verification point; a handler-side verify-then-await would reopen the supersede race); update the message in place *after* the Task method, from its returned disposition: resolved → ✅/❌ confirmation, stale mismatch → stale-prompt notice
- [x] 6.2 Add the `type === 'merge'` branch to `POST /tasks/:id/approve` (`src/connectors/api/routes.ts:253` area): extend the body contract so merge-type requests **must** carry the PR identity (`github`, `pr_number`; missing → 400), then call the same Task methods passing it as `expected`; existing `approval:resolved` emit covers the new type
- [x] 6.3 AC8 integration test: capture the registered `approve_merge`/`deny_merge` handlers (mock Bolt app recording `app.action` registrations) and invoke them with a fake body, invoke the route handler with a mock req/res (body carrying `github`/`pr_number`), and assert both paths call the identical `handleMergeApproval`/`handleMergeDenial` on the task with the same parsed `expected` identity (same resolution code including the identity check — it lives in the Task method, not the adapters); plus a mismatch case: button value naming a PR other than the one in the slot → Task method returns the stale disposition, no merge, message updated with the stale notice; plus an API case: `type: 'merge'` body missing `github`/`pr_number` → 400, no resolution call

## 7. Debug MCP extension

- [x] 7.1 Widen the `approve` tool enum to `['edit_mode', 'research_budget', 'merge']` (`tools/debug-mcp/server.ts:184`), add optional `github`/`pr_number` parameters (required when `type: 'merge'`) forwarded verbatim in the API request body to satisfy the route's merge identity contract (task 6.2), and mention merge plus the identity requirement in the tool description
- [x] 7.2 Add `'merge'` to `ApprovalType` (`tools/debug-mcp/wait-for-task.ts:14`); extend `tools/debug-mcp/wait-for-task.test.ts` with a fake feed carrying `approvalType: 'merge'` → `APPROVAL_TYPE=merge` surfaced

## 8. Docs

- [x] 8.1 `docs/architecture/github-integration.md`: add the merge-policy layer to the Merge Orchestrator section (frontmatter flag, AND semantics, ready bucket + once-per-ready-state notification, explicit-request approval path, tool gating); replace the stale `repoInfo.pr_number` legacy-fallback code sample with the current `repositories → AttachedRepo[] → branch_states` walk
- [x] 8.2 `docs/architecture/edit-mode.md`: one-line note at the `merge_pull_request` mentions (lines 15, 185) that non-auto repos add a `merge` approval gate on top of edit mode
- [x] 8.3 archie-plugins repo (`/Users/khmelev/Projects/swc/archie-plugins`, docs-only, separate PR in that repo coordinated with the engine deploy): add the `autoMerge` row to the CLAUDE.md frontmatter reference table and the multi-repo example — plain language, default-off, no harness internals
- [x] 8.4 archie-plugins PM runtime skill (`/Users/khmelev/Projects/swc/archie-plugins/pm/skills/engineering-team/SKILL.md`, same archie-plugins docs PR as 8.3 — separate repo, must ship coordinated with the engine deploy): rewrite line 28 (lifecycle step 6 "**Merge** → System auto-merges when approved + CI passes") and line 78 ("auto-merge handles the green path") for the new default — when the ready-notification finding arrives, the PM relays "PR ready — ask me to merge" to the thread once; on an explicit user request it delegates to the repo agent, whose `merge_pull_request` call surfaces approve/deny buttons the user must click; note that `autoMerge: true` repos keep the old automatic behavior and that green-CI wakeups now cover the ready notification, not auto-merge
- [x] 8.5 `prompts/repo-agent.md:142`: update the `merge_pull_request` line to describe the policy-gated contract (auto repo → merges directly after the mergeability check; non-auto repo → posts a merge-approval request to the user and pauses the task)

## 9. E2E recipe and final verification

- [x] 9.1 Add canonical recipe `merge-approval-deny` to `.claude/skills/archie-e2e/SKILL.md` §2, mirroring `edit-mode-approval`: nonce → change request opening a PR in a configured (non-auto) repo → edit-mode gate approved → PR opened → `send_message` asking to merge → `wait_for_task` asserts `STATE=approval_requested` with `APPROVAL_TYPE=merge` → `approve(type: "merge", approve: false, github, pr_number)` (identity of the PR the scenario drove open, per the API merge contract) → assert no merge occurred (denial finding in the log, no merged event/finding); include the configured-repo prerequisite + BLOCKED reporting rule from the edit-mode recipe
- [x] 9.2 Full gate: `npm run typecheck`, `npm run build`, `npm test` all green; targeted `tsc --noEmit` over the touched `tools/debug-mcp/*.ts` files (repo typecheck scopes to `src/**`)
- [ ] 9.3 Run the `merge-approval-deny` recipe against a live instance (archie-e2e skill) and write evidence to `openspec/changes/pr-merge-policy/qa-evidence/merge-approval-deny.json` + `.md` (AC3); if docker/env is unavailable, stop and return to the user before taking the brief's degradation waiver
