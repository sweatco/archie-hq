# Verification plan: github-mention-trigger

Every brief AC, its method (verbatim from the brief), the concrete scenario producing the evidence, and where the evidence lives. Unit and integration ACs are vitest cases; per the dossier's G4 finding there is no HTTP harness, so "integration" means handler-level tests driving the exported handlers (`handleGitHubMentionDirect`, `handleExistingTaskDirect`) with real payload fixtures (`src/connectors/github/__tests__/fixtures/`), a mocked `GitHubClient`, and `SESSIONS_DIR` pointed at a temp dir (the `persistence.test.ts:39` precedent) — task dirs and knowledge logs are real files. The full `npm test` run output is captured to `qa-evidence/test-run.txt` at QA time; live-e2e evidence is an `archie-e2e-evidence/v1` file pair; deploy-only is a named post-merge operator step.

| AC | Method | Scenario / check producing the evidence | Evidence location |
|----|--------|------------------------------------------|-------------------|
| AC1 | unit | `mention-routing.test.ts`: "mention in issue_comment.created with no resolving task → new_task with repo/number/commentId/author" — `routeGitHubEvent` on the `issue_comment.created` fixture with the slug set, body carrying `@{slug}`, no task on disk; assert the `new_task` route and its payload fields. Companions: "no mention → discarded exactly as today" asserts the byte-identical discard reason; word-boundary cases (`@slug-other` no match; `@slug!`/case-insensitive match); mention inside a fenced code block → detected (accepted parity, design D3); mention only in the issue title → not detected; `issue_comment.edited` with an edited-in mention → not routed; "slug unset → no mention detected". | `src/connectors/github/__tests__/mention-routing.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC2 | integration | `mention-handler.test.ts`: "authorized comment mention creates a seeded task" — `handleGitHubMentionDirect` with mocked client (`getCollaboratorPermission` → `write`) and a registry stub covering the repo, temp sessions dir; assert a task dir exists, `knowledge.log` contains repo + issue number (destination prefix), title, issue body, the mentioning comment text with `[comment_id=…]`, author, and the html link; the linked GitHub channel entry records the origin (repo/number/is_pr) **and is present in the on-disk metadata immediately after the handler returns** (sync-save durability, design D6) so `findTaskByIssueChannel` resolves the issue to the task; the PM received `AGENT_PROMPTS.newTask`. | `src/connectors/github/__tests__/mention-handler.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC3 | unit | `mention-handler.test.ts`: "read/none author → discarded silently" — permission mock returns `read` (and `none`); assert `Task.create` never invoked (no task dir), no `addPRComment`, no reaction call, and the discard reason logged. Companions: "permission lookup throws → fail-closed discard, nothing posted"; "creation throws after the permission gate → error logged, no ack comment posted". | `src/connectors/github/__tests__/mention-handler.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC4 | integration | `mention-handler.test.ts`: "issues.opened mention behaves like AC2" — the `issues.opened` fixture with the mention in the issue body routed via `routeGitHubEvent` → `new_task` (proving the newly routed `issues` event), then the handler run asserts the same task/seed/channel-origin/prompt outcomes as AC2 with the issue body as the mentioning text. Redelivery companion (router side, `mention-routing.test.ts`): a redelivered `issues.opened` for an already-mapped issue resolves via `findTaskByIssueChannel` and discards as noop — no second task, no second ack (design D1/D8). | `src/connectors/github/__tests__/mention-handler.test.ts`, `src/connectors/github/__tests__/mention-routing.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC5 | integration | `mention-handler.test.ts`: "acknowledges in-thread" — comment-born creation asserts `addCommentReaction(repo, commentId, 'eyes')` plus an `addPRComment` whose body names the created task id and contains no mention string; issue-born creation asserts `addIssueReaction(repo, issueNumber, 'eyes')` instead. Companion: "ack failure does not abort creation" (reaction mock throws → task still created, warn logged). GitHub API mocked per the brief's QA note — real-thread behavior is proven by AC12/AC13. | `src/connectors/github/__tests__/mention-handler.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC6 | integration | `github-channel.test.ts`: "PM message lands on the originating thread" — a task with a linked GitHub default channel calls `postToUser`; assert `addPRComment(repo, issue_number, …)` is called with the message + footer line, the outgoing message is logged with the `github:{repo}#{n}` destination, and the "no default channel — message dropped" warn never fires. Companions: explicit `target.channel` github delivery; `addPRComment` throws (locked issue / rate limit) → warn logged, no exception escapes `postToUser` (design D7); unconfigured client → warn, not silent null. GitHub API mocked; real-thread behavior proven by AC12/AC13. | `src/tasks/__tests__/github-channel.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC7 | integration | `mention-handler.test.ts`: "authorized follow-up comment routes to the mapped task" — with a GitHub-born task on disk, `routeGitHubEvent` on a follow-up `issue_comment.created` fixture (no mention needed) resolves `existing_task` via `findTaskByIssueChannel`; the delivery run re-checks the author (`getCollaboratorPermission` → `write`) and asserts the comment body appended to `knowledge.log` and `AGENT_PROMPTS.existingTask` sent to the PM. **Author-gate companions** (matches the updated AC7 contract): `read`/`none` follow-up author → silently dropped — no append, no PM wake, no reply, watermark NOT advanced, reason logged; permission lookup throws → dropped (fail closed) and the failure is not cached (a retry re-queries); `[bot]`-suffixed follow-up author → dropped with `getCollaboratorPermission` never called (pre-GET short-circuit, design D10); two follow-ups by the same author within the cache TTL → exactly one permission lookup, cached result gates the second (design D10 budget protection). Dedup companions: same authorized comment id delivered twice → second skipped; the triggering comment's redelivery skipped via the creation-time watermark. Slug companion (router side): slug unset → the mapping is never consulted and the follow-up does not route (design D9). | `src/connectors/github/__tests__/mention-handler.test.ts`, `src/connectors/github/__tests__/mention-routing.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC8 | unit | `mention-routing.test.ts`: "own bot comment is discarded before detection" — `issue_comment.created` fixture authored `{slug}[bot]`, including a body shaped like Archie's own acknowledgment (naming a task id) and a body containing the mention string; assert `discard: 'Own bot event'` and no `new_task`. Companion: other `[bot]`-suffixed authors produce no new-task route. | `src/connectors/github/__tests__/mention-routing.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC9 | unit | `mention-handler.test.ts`: "authorized mention in an undeclared repo declines politely" — permission mock `write`, `findAgentDefsContainingRepo` → `[]`; assert exactly one `addPRComment` with the mention-free decline text and that no task dir was created. Companions: a second authorized mention on the same thread within the dedup window posts no second decline, and a mention after the window elapses posts a fresh decline — proving expiry + lazy eviction (design D5); unauthorized author in an undeclared repo → nothing posted (authorization precedes coverage). | `src/connectors/github/__tests__/mention-handler.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC10 | unit | `readonly-github.test.ts`: "request_edit_mode declines fast on a GitHub-born task" — tool handler invoked directly (G3 factory style) on a task whose channels include a github entry (`isGitHubBorn()` true, design D6); assert the returned text states GitHub-born tasks are read-only in v1, `postInteractiveToUser` never called, no `deferTeardown`/pause, `edit_allowed` never set. Same-shaped case for `request_max_mode`. Approve-API closure companions: `task.handleEditModeApproval` on a GitHub-born task returns the rejected disposition with `edit_allowed` unset and no agent restart; the `POST /tasks/:id/approve` route layer (invoked with fake req/res per the `merge-approval-surfaces.test.ts:113-126` precedent) responds 403 and emits no `approval:resolved`. Slack-born control case: unchanged behavior. | `src/agents/__tests__/readonly-github.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC11 | unit | `mention-routing.test.ts` regression pinning: comments on `archie/task-{id}` branches still route `existing_task` with the same taskId; `pull_request_review` approved → `merge_check`, failing `check_suite.completed` → `checks_ready`, `push` → `merge_check` — all byte-identical route results on fixtures, with `determineRouteAction` untouched by the diff (asserted by the routing outcomes); plus `formatGitHubEvent` output for an Archie PR comment asserted byte-identical to today. Handler companions in `mention-handler.test.ts` (the refactored `handleExistingTaskDirect` is exported and pinned directly): an Archie-managed PR task without a github channel performs **no permission lookup** and keeps today's exact skip/advance dedup behavior (skip when `commentId <= last_processed_comment_id`, advance all branch-state matches otherwise). | `src/connectors/github/__tests__/mention-routing.test.ts`, `src/connectors/github/__tests__/mention-handler.test.ts` (+ `qa-evidence/test-run.txt`) |
| AC12 | live-e2e | archie-e2e recipe `github-mention` (new, `.claude/skills/archie-e2e/SKILL.md` §2): boot the dockerized instance from this branch with dev GitHub App credentials (`GITHUB_APP_ID/SLUG/PRIVATE_KEY_PATH/INSTALLATION_ID/WEBHOOK_SECRET`) and a plugin-covered test repo; create a real issue in the test repo; POST a correctly signed (HMAC-SHA256) synthetic `issue_comment.created` payload referencing that issue, body mentioning the dev slug, author a real write-permission user; `wait_for_task`/`list_tasks` via the archie-debug MCP until the task appears, `get_log` asserts the seeded knowledge log (title, body, comment, link); then fetch the issue via the GitHub API and assert the 👀 reaction and the acknowledgment comment naming the task are visible on the real issue. Prerequisite-missing rule: report BLOCKED and stop — per the brief's QA limitation, degrade to `manual` or an explicit waiver naming AC13 as fallback **only with the user's say-so**. | `openspec/changes/github-mention-trigger/qa-evidence/github-mention.json` (+ rendered `.md`) |
| AC13 | deploy-only | Named post-merge step (operator: Igor, sign-off owner). In the production GitHub App settings: subscribe the `issues` webhook event (keep `issue_comment`) and confirm Issues: read & write permission (reactions and plain-issue comments require Issues: write per the dossier H2/H3). Then post one real mention as a write-permission user in a plugin-covered repo and confirm: a task is created, the 👀 + acknowledgment comment appear on the thread, and a PM reply lands as a comment. Unverifiable pre-merge by nature (App settings console change). | Post-merge checklist comment on the PR (task id, issue link, screenshot/permalink of the ack); AC marked verified in `forge.yaml` afterwards |

Cross-cutting gates (not tied to one AC): `npm run typecheck`, `npm run build`, `npm test` green; the boot warning for a set `GITHUB_WEBHOOK_SECRET` with an unset `GITHUB_APP_SLUG` observed in the AC12 boot log (or a targeted unit assertion); `lifecycle.test.ts` asserts the summary links block renders `/issues/`-vs-`/pull/` URLs from the reshaped GitHub channel (the removed `pr_number` cast, task 1.1); docs tasks (9.1-9.2) present in the diff.
