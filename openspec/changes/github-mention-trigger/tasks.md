## 1. Types, channel surface, and PM delivery (design D6, D7)

- [x] 1.1 Reshape `GitHubChannel` in `src/types/task.ts` (`{ type: 'github'; repo; issue_number; is_pr; last_processed_comment_id? }` — safe, never constructed per J3) and update the two read-side consumers: `spawn.ts` `renderChannel` (`GitHub {repo}#{n} (PR|issue)`) and `memory/lifecycle.ts` `buildLinksBlock` — **remove the `(channel as { pr_number?: number }).pr_number` cast at `lifecycle.ts:378-379`** (the else-if already narrows; the cast would turn the reshape into a silent undefined) and build `/pull/` vs `/issues/` URLs from `is_pr`. Extend `src/memory/__tests__/lifecycle.test.ts` (via the exported `buildSummaryMarkdown`) asserting the github links render for both `is_pr` values. `npm run typecheck` green.
- [x] 1.2 Add `Task.linkGitHubChannel(repo, issueNumber, isPr)` and `Task.isGitHubBorn()` (any channel with `type === 'github'` — the single readonly/origin predicate, design D6) in `src/tasks/task.ts`, mirroring `linkCliChannel` (`task.ts:411-415`): channel key `github:{repo}#{issueNumber}`, `default_channel ??=` promotion, idempotent re-link. Tests in new `src/tasks/__tests__/github-channel.test.ts` (temp `SESSIONS_DIR` per `persistence.test.ts:39`): entry shape, key, promotion only when default unset, idempotency, `isGitHubBorn` true/false.
- [ ] 1.3 Add the `github` branch to `postToUser` in both arms (default channel and explicit `target.channel`) posting via `getGitHubClient().addPRComment(repo, issue_number, message + footer-line)` — the call wrapped in try/catch that warns and continues (locked/closed/transferred issue, rate limit) instead of propagating into the PM's tool call — with `logOutgoingMessage` destination `github:{repo}#{n}` and a warn (not silent null) when the client is unconfigured; add the `postFilesToUser` github warn ("files dropped"). Tests in `github-channel.test.ts` with mocked client: message lands as a comment on the originating thread, never the "no default channel — message dropped" path (AC6); explicit-target github delivery; `addPRComment` throws → warn logged, no exception escapes; null-client warn.

## 2. GitHub client helpers (design D4, D9; C2)

- [ ] 2.1 Add `getCollaboratorPermission(githubRepo, username)` to `src/connectors/github/client.ts` wrapping `GET /repos/{owner}/{repo}/collaborators/{username}/permission`, returning the legacy `permission` value (`admin|write|read|none`; maintain→write, triage→read per H4), and reaction helpers `addCommentReaction(githubRepo, commentId, content = 'eyes')` (`POST /repos/{o}/{r}/issues/comments/{comment_id}/reactions`) and `addIssueReaction(githubRepo, issueNumber, content = 'eyes')` (`POST /repos/{o}/{r}/issues/{issue_number}/reactions`) per H3. Unit tests in new `src/connectors/github/__tests__/client-helpers.test.ts` with a stubbed octokit `request` asserting endpoint paths, params, and the permission passthrough.

## 3. Router: context, detection, route variant (design D1, D2, D3)

- [ ] 3.1 Extend `formatGitHubContext` in `src/connectors/github/webhooks.ts`: `GitHubEventContext.issueNumber?`, populate it (plus `body`/`commentId`) for all `issue_comment` events while keeping `prNumber` gated on `issue.pull_request` (A2/H1), and add the `issues` branch (A3: `issueNumber`, `body`, `state`). Update `formatGitHubEvent`'s `issue_comment` case to render destination `issue #N` when only `issueNumber` is set (`PR #N` rendering unchanged when `prNumber` exists — AC11).
- [ ] 3.2 Add mention detection and the `new_task` route to `webhooks.ts`: `matchesMention(body, slug)` with the H6 word-boundary regex (case-insensitive, local `escapeRegExp`), eligibility limited to `issue_comment.created` and `issues.opened` (edited actions never eligible), inert when `GITHUB_APP_SLUG` is unset (A6), `[bot]`-suffixed authors skipped (design Decision 3); `GitHubMentionEvent` extraction from the raw payload (repo, issueNumber, isPr, author, commentId?, commentBody?, issueTitle, issueBody?, issueAuthor?, htmlUrl); the `new_task` variant on `GitHubRouteResult`, intercepting in `routeGitHubEvent` exactly at the no-taskId discard (`webhooks.ts:469-471`, after the self-filter and all resolution attempts per A5) — otherwise the byte-identical discard, `determineRouteAction` untouched.
- [ ] 3.3 Router unit tests in new `src/connectors/github/__tests__/mention-routing.test.ts` with payload fixtures under `__tests__/fixtures/` (octokit examples, H1): mention in `issue_comment.created` with no resolving task → `new_task` carrying repo/number/commentId/author, no mention → discard exactly as today (AC1); `{slug}[bot]` comment incl. an ack-shaped body → `Own bot event` discard (AC8); word-boundary cases (`@slug-other` no match, `@slug!` match, case-insensitive); mention inside a fenced code block → detected (accepted claude-code-action parity); mention only in the issue *title* → not detected; `issue_comment.edited` with a newly edited-in mention → discarded; slug unset → discard + filter off; mention on an Archie-managed PR branch → `existing_task` unchanged, `merge_check`/`checks_ready` fixtures route byte-identically, and `formatGitHubEvent` output for an Archie PR comment is byte-identical (AC11 regression pinning).

## 4. Issue→task mapping (design D8, D9; B8)

- [ ] 4.1 Add `findTaskByIssueChannel(githubRepo, issueNumber)` to `src/tasks/persistence.ts`: in-memory `activeTasks` github-channel check first (per `findTaskByThread`), then `scanMetadataFiles('"issue_number": N')` with structural verification (channel `type === 'github'`, repo + number match). Wire it into `routeGitHubEvent` for **both** `issue_comment` (third resolution step after the PR-number fallback, `webhooks.ts:458-460`) **and** `issues` events (so a redelivered `issues.opened` resolves and discards via `noop` instead of re-creating a task, design D1/D8), with the consult **slug-gated** (skipped entirely when `GITHUB_APP_SLUG` is unset — GitHub-born routing inert like detection, design D9). Tests: persistence cases in `src/tasks/__tests__/persistence.test.ts` (found via channel entry, repo mismatch → null, substring hit without a matching channel → null); router cases in `mention-routing.test.ts` (redelivered `issues.opened` for a mapped issue → noop discard, no `new_task`; slug unset → mapping never consulted, github-born follow-up does not route).

## 5. Mention handler: gates, creation, seeding, ack (design D4, D5, D6, D8, D9)

- [ ] 5.1 Add `handleGitHubMentionDirect(mention)` to `src/connectors/github/events.ts` (exported for tests) and its dispatch case in `handleGitHubWebhook`: null-client or permission-API failure → fail-closed logged discard; `getCollaboratorPermission` gate accepting `admin|write` only, `read`/`none` → logged discard, no task, no reply (AC3); then `findAgentDefsContainingRepo` coverage gate → mention-free polite decline via `addPRComment`, deduplicated by an in-memory `repo#issue` → timestamp map with a ~10-minute window and lazy eviction of expired entries on access so repeated mentions can't spam declines and the map can't grow unboundedly (design D5), no task (AC9); then the D6 re-check of `findTaskByIssueChannel` falling through to existing-task delivery on a race. Test note (in 5.3's AC9 cases): a mention after the window elapses posts a fresh decline (proves expiry + lazy eviction).
- [ ] 5.2 Implement the creation path in `handleGitHubMentionDirect`: `Task.create()` → `linkGitHubChannel` → **`await task.save(true)`** (synchronous flush — the channel entry is the readonly marker and must not sit in the debounce window, design D6) → seed knowledge.log per D8 (context entry with title/body/link; comment-born adds the verbatim comment entry with `[comment_id=…]` and sets the channel watermark to the triggering comment id) → `metadata.title ??= issueTitle` → ack (👀 via `addCommentReaction`/`addIssueReaction` + short mention-free comment naming the taskId; failures warn, never abort) → `sendMessage(AGENT_PROMPTS.newTask, 'pm-agent')`.
- [ ] 5.3 Handler tests in new `src/connectors/github/__tests__/mention-handler.test.ts` (mocked client + registry, temp `SESSIONS_DIR`, real fixtures — G4 shape): authorized comment mention → task created, knowledge.log contains repo/number/title/body/comment/author/link, the github channel entry is present **in the on-disk metadata immediately after the handler returns** (sync-save assertion), PM got the new-task prompt (AC2); `issues.opened` mention → same outcomes with issue-reaction ack (AC4); ack calls = reaction + comment naming the task (AC5); `read`/`none` author → no `Task.create`, no comment, no reaction, reason logged (AC3); uncovered repo → decline comment posted once for two mentions within the window, no task (AC9); permission-API throw → fail-closed discard; creation throws after the permission gate (e.g. `Task.create` rejects) → error logged, no ack comment posted.

## 6. Follow-up routing: author gate and dedup (design D10; A9)

- [ ] 6.1 Extend `handleExistingTaskDirect` in `events.ts` (**export it for tests** — it is module-private today) for the GitHub-born path (task's matching thread is a github channel): (a) **author permission gate** — short-circuit `[bot]`-suffixed authors BEFORE any permission GET (design D10; mirrors Decision 3's detection skip), then re-check the author's permission **through a short-TTL in-memory per-`(repo, author)` cache** (~5 min, caches resolved outcomes including `read`/`none`, never caches thrown lookups, lazy eviction on access — design D10: ungated GETs would spend the shared installation token's primary budget) and silently drop `read`/`none` or lookup-failure authors (logged reason, no append, no PM wake, watermark NOT advanced); (b) dedup — fold the channel's `last_processed_comment_id` into the existing max/skip/advance computation (keyed on `issueNumber ?? prNumber`), advancing channel and branch-state stores together; plain-issue comments (no `prNumber`) now dedup via the channel. Tests in `mention-handler.test.ts`: authorized follow-up (no mention needed) routes to the mapped task, appends, pings the PM with the existing-task prompt (AC7); `read`/`none` follow-up author → dropped silently, no append, no PM wake, watermark unchanged (AC7 author gate); permission lookup throws → dropped (fail closed), and the failure is NOT cached (a retry re-queries); `[bot]` follow-up author → dropped with `getCollaboratorPermission` never called; two follow-ups by the same author within the TTL → exactly one permission lookup (cache); redelivered same comment id → skipped; Archie-managed PR task without a github channel → **no permission lookup performed** and skip/advance dedup byte-identical to today on the refactored handler (AC11 companion pin).

## 7. Readonly v1 enforcement (design D11; E2-E5, J5)

- [ ] 7.1 Add `isGitHubBorn()` fail-fast guards at the top of `request_edit_mode` and `request_max_mode` in `src/agents/tools.ts`: explanatory ok-return, no interactive post, no `suspendStatus`, no `deferTeardown` (AC10). Tests in new `src/agents/__tests__/readonly-github.test.ts` (G3 factory style): both tools on a GitHub-born task → declined text, `postInteractiveToUser` never called, no pending teardown, `edit_allowed` never set; same tools on a Slack-born task → unchanged behavior.
- [ ] 7.2 Guard `handleEditModeApproval` in `src/tasks/task.ts`: `isGitHubBorn()` → warn + decision finding + return `'rejected_readonly'` disposition without touching `edit_allowed`/`edit_approved_by`/agent restarts (widen the return type per the `handleMergeApproval` pattern); map the disposition to `403` in `POST /tasks/:id/approve` (`routes.ts:274-279`) skipping the `approval:resolved` emit; Slack `approve_edit_mode` handler tolerates the disposition. Tests in `readonly-github.test.ts`: direct approval call on a GitHub-born task → rejected, `edit_allowed` unset, finding appended; route-level test (G4 `merge-approval-surfaces` style) → 403 body, no `approval:resolved`; Slack-born task approval unchanged.

## 8. Boot warning, PM context, prompts (design D9, D12, D13)

- [ ] 8.1 Add the boot warning in `src/index.ts` (webhook mounted with `GITHUB_WEBHOOK_SECRET` but `GITHUB_APP_SLUG` unset → "self-event filter, mention trigger, and GitHub-born task routing disabled"), and annotate `GITHUB_APP_SLUG` in `.env.example` as load-bearing for all three.
- [ ] 8.2 Add the GitHub-born PM context line in `src/agents/spawn.ts` keyed off `isGitHubBorn()` (`triggered_by` precedent: origin repo/number/URL from the github channel entry, replies land on the GitHub thread, read-only for the task's lifetime, never request edit/max mode) and the "GitHub-born tasks" subsection in `prompts/pm-agent.md` (same rules + Slack-specific tools do not apply). Typecheck green; context line covered by an assertion in `readonly-github.test.ts` or a small spawn-context test.

## 9. Docs, e2e recipe, final gate (design D13, D14; verification plan)

- [ ] 9.1 Update `docs/architecture/github-integration.md`: new "Mention trigger" section (route variant, permission gate for mentions AND follow-ups, coverage gate + decline dedup, GitHub channel surface, ack, slug-gated loop safety, readonly v1) and refresh the stale Webhook Router section (add `check_suite`/`checks_ready` and `status` rows, fix the "three internal actions" prose); one-line trust-boundary note in `docs/architecture/security.md` (GitHub mention/follow-up ingress gated by repo write permission); superseded-by banner on `docs/proposals/github-mention-workflow.md` (I1).
- [ ] 9.2 Add the `github-mention` recipe to `.claude/skills/archie-e2e/SKILL.md` §2 (per the verification plan: dev-app prerequisites + BLOCKED reporting rule, real test issue, signed synthetic `issue_comment` POST, archie-debug MCP observation, real-GitHub ack assertion).
- [ ] 9.3 Full gate: `npm run typecheck`, `npm run build`, `npm test` all green.
- [ ] 9.4 Run the `github-mention` recipe against a live instance (archie-e2e skill) and write evidence to `openspec/changes/github-mention-trigger/qa-evidence/github-mention.json` + `.md` (AC12); if dev GitHub App credentials or the test repo are unavailable, stop and return to the user before taking the brief's degradation path (manual run or waiver naming AC13 as fallback).
