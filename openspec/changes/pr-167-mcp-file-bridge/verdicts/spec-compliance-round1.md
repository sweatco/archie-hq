# Spec-compliance review — round 1

**Verdict: FINDINGS (1 non-blocking; no blocking findings — treated as PASS for stage exit)**

1. **Non-blocking** — AC6 / brief claims the target tool's response is "surfaced verbatim (incl. isError)", but the implementation deviates in two small ways: (a) `isError` results are wrapped with a prefix rather than passed through — `err(`"${args.tool_name}" on "${args.server}" reported: ${text}`)` at src/agents/mcp-file-bridge.ts:175; (b) `extractResultText` drops non-text content parts (images, resources) and joins/trims text parts at src/agents/mcp-file-bridge.ts:55-62, so a non-text response degrades to "(tool returned no text content)". The response text itself is fully preserved and the run's own tests (src/agents/__tests__/mcp-file-bridge.test.ts:243-253) pin the wrapped form, so the intent of the criterion is met; this is a precision deviation, not a defect in behavior.

Everything else verified clean: T1 (multi-file `files: [{path, argument}]` schema, sum ceiling, collision-win, updated description — src/agents/mcp-file-bridge.ts:98-116, 129-146, 168), T2 (only `type: 'http'` with URL accepted; sse and stdio rejected explicitly — src/agents/mcp-file-bridge.ts:110-117), T3 (all four new test cases present — src/agents/__tests__/mcp-file-bridge.test.ts:100-124, 141-156, 158-174, 216-238), T4 (exported `shouldAttachFileBridge` on real `isPmAgent`/`isRepoAgent` predicates — src/agents/mcp-file-bridge.ts:38-40; wired in spawn.ts:520-521; predicate exactly matches the PM/repo/else branch structure at spawn.ts:281/360; gating tests at src/agents/__tests__/mcp-file-bridge.test.ts:248-266), T5 (re-ran: `npm run typecheck` clean, `npm test` 606 passed / 41 files, matching the claim). AC2-AC5 each have a passing unit test asserting both the error text and that no connect/read happens. Non-goals hold: no PM/repo access, no stdio/SSE forwarding, no new credential plumbing (headers reused from `agent.def.mcpServers`), no presigned-URL path, no image-swap prompts/skills. No scope creep: the diff is exactly the declared blast radius (2 new files + 9-line wiring in src/agents/spawn.ts); the duplicate-target-argument guard (src/agents/mcp-file-bridge.ts:120-124) is a necessary semantic of the T1 multi-file schema, not a drive-by addition. AC7/AC8 are live-e2e/deploy-only and correctly have no diff footprint.

**Disposition:** finding 1 accepted as-is — the wrapped error prefix is deliberate (gives the agent actionable context) and non-text MCP content is out of scope for the bridge's first consumers. Brief's AC6 wording ("response surfaced verbatim") interpreted as: text content and error-ness faithfully relayed, which holds.
