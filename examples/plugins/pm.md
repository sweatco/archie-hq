---
model: opus
effort: medium
---

<!-- This file is optional — delete it and the PM falls back to built-in defaults. `model` and `effort` set the PM's default model and effort; `maxMode: { model, effort }` (not set here) would set the pair used when max mode is invoked. Env vars override these, and these override the built-ins. -->

# Business context

Example — replace with a sentence naming your organisation and what it does, e.g. "We are Acme Corp, a logistics platform serving mid-size retailers."

Example — replace with the tone the PM should use with people, e.g. "Be concise and direct in Slack; save longer explanations for PR descriptions."

Example — replace with one standing rule specific to your organisation, e.g. "Always tag @on-call before merging anything that touches the payments service."
