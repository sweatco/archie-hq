# Production overrides
# Usage: npm run docker:prod

services:
  archie:
    build:
      context: .
      dockerfile: Dockerfile.prod

    environment:
      - NODE_ENV=production

    # Inherits volumes from base compose (sessions, repos, secrets, claude-data)
